import 'package:flutter/material.dart';

class InfiniteScroll extends StatelessWidget {
  final name = 'Infinite Scroll';
  const InfiniteScroll({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
